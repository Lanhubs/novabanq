import 'package:flutter/material.dart';

class AccountTagAlertIcon extends StatelessWidget {
  const AccountTagAlertIcon({super.key});

  @override
  Widget build(BuildContext context) {
    return const Icon(
      Icons.warning_rounded,
      size: 86,
      color: Color(0xFFFDB022),
    );
  }
}
